# Development override for docker-compose.yml
services:
    backend:
        build:
            target: production
        environment:
            - NODE_ENV=development
            - DEBUG=true
        volumes:
            - ./backend:/app
            - /app/node_modules
        command: npm run dev

